import { ref as i, watch as v, onBeforeUnmount as u } from "vue";
function f(e) {
  const r = { interval: null }, t = i(0), l = () => {
    r.interval && (clearInterval(r.interval), r.interval = null);
  }, a = () => {
    t.value = Math.min(t.value + o(t.value), 100);
  }, n = () => {
    l(), e.active ? (t.value = 0, r.interval = setInterval(() => a(), 300)) : (t.value = 100, r.interval = setInterval(() => t.value = 0, 500));
  };
  return v(() => e.active, () => {
    n();
  }), u(() => l()), e.active && n(), {
    value: t
  };
}
function o(e) {
  return e < 20 ? 10 : e < 50 ? 4 : e < 80 ? 2 : e < 99 ? 0.5 : 0;
}
export {
  f as default
};
